import { FC, useMemo } from 'react';
import { View, Text } from 'react-native';

const Homepage: FC = () => {
  return (
    <View style={$tw('')}>
      <Text>首页</Text>
    </View>
  );
};

export default Homepage;
